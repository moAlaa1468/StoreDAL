package store_database.dal.hibernate;

import com.mysql.cj.xdevapi.SessionFactory;
import org.hibernate.Session;
import org.hibernate.engine.jdbc.connections.internal.ConnectionCreatorBuilder;

public class HibernateDBManager {

    // property For the session that will be created
    private static SessionFactory dbSessionFactory;

    /*
     * This will create a session auAlaa
     * */
    public void buildSessionFactory() {
        if (dbSessionFactory == null) {
            return;

        }
    }
}













